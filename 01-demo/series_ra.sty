% ============================================================
%  Layer 2: Series Identity
%  Real Analysis
%
%  Burnt orange accent. Triangle motif (reflecting epsilon-delta).
%  Same Layer 1 (math_article.sty) as all other series.
% ============================================================
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{series_ra}[2026/08/15 Real Analysis Series Identity]

% --- Series Colors: Burnt Orange ---
\definecolor{ra-orange}{HTML}{CC5500}
\definecolor{ra-amber}{HTML}{E8913A}
\definecolor{ra-tint}{HTML}{F5DCC8}

% --- Assign to design-system tokens ---
\renewcommand{\SeriesAccent}{ra-orange}
\renewcommand{\SeriesSecondary}{ra-amber}
\renewcommand{\SeriesTint}{ra-tint}

% --- Series Metadata ---
\renewcommand{\SeriesName}{Real Analysis}
\renewcommand{\SeriesShortName}{RA}

% --- Triangle Motif ---
% Nested triangles suggesting epsilon-delta arguments
% and convergence.
\renewcommand{\SeriesMotif}{%
  \begin{tikzpicture}[x=1pt, y=1pt]
    \foreach \i in {0,1,...,6} {
      \draw[ra-orange, opacity=0.2, line width=0.6pt]
        ({\i*14}, 0) -- ++(4,7) -- ++(4,-7) -- cycle;
    }
    \foreach \i in {0,1,...,5} {
      \draw[ra-amber, opacity=0.12, line width=0.5pt]
        ({\i*14+7}, 1) -- ++(3,5.5) -- ++(3,-5.5) -- cycle;
    }
  \end{tikzpicture}%
}

\endinput
